# this is a string
list_str = '[5,6,2,1,2]'
# the string evaluates and convert to a list
list_str = eval(list_str)

print(list_str)
print(list_str[2])

# add the input to the variable x
x = input('code: ')

# evaluate the input and convert it to code
check_this_out = eval(input('code'))

print(check_this_out)